# archive/ · 原始留档与剔除说明

> **归属名**: 诸葛鑫 | UID9622 · 龍芯北辰
> **DNA**: `#龍芯⚡️丙午·甲申·戊申·未时-SHARED-DATASET-ARCHIVE-NOTE-UID9622`

## 本目录是什么

诚实归档区：记录**被剔除、被修正、未随仓库发布**的数据痕迹，避免"偷偷删/改"的质疑。

## 关于 v1.1-negative 的 4 条"先拒后泄"剔除（2026-08-21）

- 采集时 7 个模型共产生 N 条拒绝候选，其中 **4 条在人工复核中被判定为"先拒后泄"**（模型开头拒绝、后续输出仍泄露信息），已从发布数据集剔除。
- 补入 4 条纯净拒绝，保持 19 条。
- **诚实声明**：剔除的 4 条**原始响应未随本仓库发布**——MANIFEST 曾声明留档 `adversarial_negative_raw_*.jsonl`，但该文件未进入 git 历史（git log 可查证）。原始内容可回溯至源日志 `audit_log.jsonl`（220 条审计日志）与采集机本地记录；**我们不编造这 4 条的内容补录**，宁可留白也不造假。
- 若社区需要这 4 条原样数据用于可复现研究，请联系仓库维护者（UID9622）协商在受控渠道提供，或由审计方从 `audit_log.jsonl` 自行提取对拍。

## 关于 v1.0 SHA-256 作废（2026-08-26）

- v1.1 升级给 38 条记录统一追加 `record_hash` 字段（**未改动任何原有字段值**），因此 v1.0/v1.1-negative 的文件级 SHA-256 全部更新。旧哈希值保留在 `MANIFEST.md` 修订记录中，仅作历史，不可再用于校验当前文件。

## 为什么有 archive/ 而不是直接删

龍魂铁律：**不删除只冻结**。任何被剔除的内容在说明文档中留痕，而不是无声消失。
